/*
Create variables or constants named userArray. Store "fullName", "age", "gender" and "phoneNumber" in userArray as Array. Show the values of those in the terminal as following format.
*/

const userArray = ["Your Full Name", "0", "Male/Female", "9876543210"];

console.log(`Full Name: ${userArray[0]}`);
console.log(`Age: ${userArray[1]}`);
console.log(`Gender: ${userArray[2]}`);
console.log(`Phone Number: ${userArray[3]}`);
